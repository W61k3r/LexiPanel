{
  "private": true,
  "description": "Browser checks for the panel UI (tests/ci/ui.sh). npm install here, then npx playwright install chromium.",
  "devDependencies": {
    "jsdom": "^24.1.3",
    "playwright": "1.56.1"
  }
}
